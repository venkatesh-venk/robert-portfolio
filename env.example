# Environment Variables for Robert Portfolio
# Copy this file to .env and update the values as needed

# Application Configuration
NODE_ENV=production

# Next.js Public Variables (exposed to browser)
NEXT_PUBLIC_LAB_URL=http://localhost:3000

# Docker Configuration
COMPOSE_PROJECT_NAME=robert-portfolio
COMPOSE_FILE=docker-compose.yml

# Development vs Production
# For development: COMPOSE_FILE=docker-compose.yml:docker-compose.override.yml
# For production: COMPOSE_FILE=docker-compose.yml:docker-compose.prod.yml

# Port Configuration (optional override)
# APP_PORT=3000

# Health Check Configuration
# HEALTH_CHECK_TIMEOUT=10s
# HEALTH_CHECK_INTERVAL=30s
